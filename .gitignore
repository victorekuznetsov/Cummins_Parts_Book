# сессия браузера: куки и токены — в репозиторий не попадают
session/

# рабочие логи и временные файлы
*.log
*.part
__pycache__/

# исходные картинки выгрузки не храним: ровно те же файлы лежат в catalog/
# (drawings/ и parts/) и при необходимости заново качаются crawler.py
data/*/drawings/
data/*/parts/
